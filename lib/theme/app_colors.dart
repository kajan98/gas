import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primaryBlue = Color(0xFF344CB7);
  static const Color white = Colors.white;
  static const Color black = Colors.black;

  // Secondary Colors
  static final Color secondaryBlue = primaryBlue.withOpacity(0.8);
  static final Color lightBlue = primaryBlue.withOpacity(0.1);

  // Background Colors
  static const Color backgroundLight = white;
  static const Color cardBackground = white;

  // Text Colors
  static const Color textPrimary = black;
  static const Color textSecondary = Colors.black54;

  // Button Colors
  static const Color buttonBlue = primaryBlue;

  // Icon Colors
  static const Color iconBlue = primaryBlue;

  // Shadow Colors
  static final Color shadowColor = primaryBlue.withOpacity(0.1);
} 